using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Professions enumerators. Never change values of already initiated values (OnClick actions may break).
/// </summary>
public enum Professions
    {
        Unemployed = 0,
        Woodcuter = 1,
        FoodGatherer = 2,
        Miner = 3,
    }
